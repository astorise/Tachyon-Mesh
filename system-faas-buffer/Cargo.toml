[package]
name = "system-faas-buffer"
version = "0.1.0"
edition = "2021"

[lib]
name = "buffer"
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.46"
