[package]
name = "guest-example"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
faas-sdk = { path = "../faas-sdk" }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["fmt", "json"] }
