[package]
name = "system-faas-dist-limiter"
version = "0.1.0"
edition = "2021"

[lib]
name = "dist_limiter"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.57"
