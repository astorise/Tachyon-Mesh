[package]
name = "system-faas-dist-limiter"
version = "1.0.0"
edition = "2024"
license.workspace = true

[lib]
name = "dist_limiter"
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.62"
