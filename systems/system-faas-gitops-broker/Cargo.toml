[package]
name = "system-faas-gitops-broker"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
gix = { version = "0.85", default-features = false, features = ["sha1"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.59"
