[package]
name = "system-faas-node-registry"
version = "1.1.0-alpha"
edition = "2021"
build = "build.rs"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
wit-bindgen = "0.57"
