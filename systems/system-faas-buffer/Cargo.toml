[package]
name = "system-faas-buffer"
version = "1.0.0"
edition = "2021"
license.workspace = true

[lib]
name = "buffer"
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.62"
