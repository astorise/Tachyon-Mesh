[package]
name = "system-faas-config-api"
version = "1.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
parallel-topology = { path = "../../crates/parallel-topology" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wit-bindgen = "0.58"
