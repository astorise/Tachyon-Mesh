- [x] **Task 1: WIT Bindings Integration.** Add `wit-bindgen` to `tachyon-ui/Cargo.toml`. Replace the manually defined `#[derive(Deserialize)]` configuration structs in `tachyon-ui/src/main.rs` with types automatically generated from `../wit/config-*.wit`.
- [x] **Task 2: MCP Authentication & Limits.** Update `tachyon-mcp/src/main.rs` to require a `--token` (PAT) for connection. Implement a basic token-bucket rate limiter for write tools (`tachyon_register_resource`, `tachyon_seal_overlay`, `tachyon_apply_manifest`).
- [x] **Task 3: UI Security Audit & Fixes.** Scan all UI components for unescaped `innerHTML` assignments. Replace them with `textContent` or use a strict DOM sanitizer. In `tachyon-ui/src/main.rs`, replace `.expect("error while running tauri application")` with a `match` that logs to standard error and exits with code 1.
- [x] **Task 4: Error Localization Module.** Enhance `tachyon-ui/src/utils/i18n.ts` with an `ErrorTranslator` class. It must use regex or error code mapping to intercept raw Rust error strings (e.g., "WIT validation failed: kv_cache_size must be...") and return clean, localized equivalents before they hit the `TachyonToastManager`.
