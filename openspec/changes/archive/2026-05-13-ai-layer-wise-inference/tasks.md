- [x] **Task 1: WIT Update.** Update `wit/ai/inference.wit` to include the `memory-profile` enum (`performance` vs `layer-wise-streaming`) within `inference-options`. Ensure generated bindings are updated.
- [x] **Task 2: Zero-Copy Model Loader.** Refactor model loading in `core-host/src/ai_inference.rs` to use `memmap2` for zero-copy `.safetensors` mapping when `layer-wise-streaming` is requested. Do not load all tensors to the GPU device at startup.
- [x] **Task 3: Prefill Batching Implementation.** Implement a distinct `prefill` logic block in the inference loop. It must process all input prompt tokens through Layer 1, save the activations, drop Layer 1 from VRAM, load Layer 2, and repeat, calculating the entire prompt context in a single model traversal.
- [x] **Task 4: Asynchronous Pipeline & KV-Cache Swap.** Implement the autoregressive decode loop using a Ring Buffer pattern. Utilize Candle's async device operations (or `cudarc` streams) to fetch Layer $N+1$ to VRAM while computing Layer $N$. Concurrently offload the KV-Cache slice of Layer $N$ to Host RAM (or `kv-partition` V2) to maintain strict $O(1)$ VRAM footprint per layer.