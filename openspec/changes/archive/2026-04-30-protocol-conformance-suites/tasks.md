# Implementation Tasks
- [x] Add `guest-grpc-interop` as a new example module that implements the official gRPC testing contract.
- [x] Add the `gRPC Interop` job to `.github/workflows/integration.yml`.
- [x] Set up the `quic-interop-runner` harness in a new `tests/conformance/quic/` directory.
- [x] Add the `QUIC Interop` job to `.github/workflows/integration.yml`.
- [x] Generate dynamic SVG badges (e.g., "gRPC Conformance: 100%") to display on the `README.md`.
