# Tachyon Mesh Benchmark Notes

Use the Tachyon Mesh namespace as the no-sidecar baseline for the neutral echo workload. Tachyon-specific adapters can be added here once the benchmark target is deployed by the release pipeline.

The benchmark target is `http://echo.tachyon-bench.svc.cluster.local`.
